// Viola la regla a proposito: la aplicacion solo depende del dominio.
import { TypeOrmDatabaseProbe } from '@/infrastructure/database/typeorm-database.probe'

export const violacion = TypeOrmDatabaseProbe
